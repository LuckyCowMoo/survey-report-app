/** Time focused on a yellow / note-confirm pip (or striped tag) to finish the fill. */
export const PIP_DWELL_MS = 5000;
/** Time for a partial fill to drain back after leaving early. */
export const PIP_REVERSE_MS = 2800;
/** Brightness pulse when a pip / striped tag fill completes. */
export const PIP_FLASH_MS = 480;
